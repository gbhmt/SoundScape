export const eachUserTrack = (tracks) => {
  return Object.keys(tracks).map((key) => {
    return tracks[key];
  });
};

export const allTracks = (tracks) => {
  return Object.keys(tracks).map((key) => {
    return tracks[key];
  });
};
